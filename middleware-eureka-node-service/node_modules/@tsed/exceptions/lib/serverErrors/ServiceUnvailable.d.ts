import { Exception } from "../core/Exception";
export declare class ServiceUnvailable extends Exception {
    static readonly STATUS = 503;
    name: string;
    constructor(message: string, origin?: Error | string | any);
}
