import { Exception } from "../core/Exception";
export declare class RequestedRangeUnsatisfiable extends Exception {
    static readonly STATUS = 416;
    name: string;
    constructor(message: string, origin?: Error | string | any);
}
