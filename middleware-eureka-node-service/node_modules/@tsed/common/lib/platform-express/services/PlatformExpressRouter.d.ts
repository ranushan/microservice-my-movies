import { Configuration } from "@tsed/di";
import * as Express from "express";
import { PlatformHandler, PlatformRouter } from "../../platform";
declare global {
    namespace TsED {
        interface Router extends Express.Router {
        }
    }
}
export declare class PlatformExpressRouter extends PlatformRouter {
    constructor(platform: PlatformHandler, configuration: Configuration, routerOptions: any);
}
