import { InjectorService } from "@tsed/di";
import { ControllerProvider } from "../domain/ControllerProvider";
import { IPlatformDriver } from "../interfaces/IPlatformDriver";
export declare class ControllerBuilder {
    private provider;
    constructor(provider: ControllerProvider);
    /**
     *
     * @returns {any}
     */
    build(injector: InjectorService): IPlatformDriver;
    private buildEndpoints;
    private buildEndpoint;
    private buildChildrenCtrls;
    private buildMiddlewares;
}
